management.endpoint:
  shutdown:
    access: none

management.endpoints:
  web:
    exposure:
      include: health,info,beans,metrics

#spring.application:
#  name: Rick and Morty API Spring Data REST

spring.datasource:
  driver-class-name: org.sqlite.JDBC
  #  url: jdbc:sqlite::resource:rickandmortyapi.db?jdbc.explicit_readonly=true
  url: jdbc:sqlite::resource:rickandmortyapi.db
#  username:
#  password:

spring.jpa:
  show-sql: true
  properties:
    hibernate:
      format_sql: true
      highlight.sql: true
      highlight.sql_logging: true

